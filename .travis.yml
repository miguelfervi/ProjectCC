language: python
python:
  - '2.6'
  - '2.7'
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script:  npm install mocha --save-dev; npm install chai --save-dev; npm install chai-fs --save-dev;npm install supertest --save-dev;  npm test
